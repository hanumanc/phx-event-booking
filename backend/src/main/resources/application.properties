# Application Configuration
spring.application.name=event-booking-backend
server.port=8080

# MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://phx-event-booking-user:0T4kt8elXXH1s2UV@phx-java-training.0lo6ihm.mongodb.net/?retryWrites=true&w=majority&appName=phx-java-training}
spring.data.mongodb.database=event_booking_db

# JWT Configuration
jwt.secret=${JWT_SECRET:mySecretKey}
jwt.expiration=86400000

# CORS Configuration
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200}

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Logging Configuration
logging.level.com.eventbooking=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG